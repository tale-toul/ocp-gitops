apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: ghost
  name: ghost
spec:
  host: {{ .Values.route.hostname }}.{{ (lookup "operator.openshift.io/v1" "IngressController" "openshift-ingress-operator" "default" ).status.domain }} 
  port:
    targetPort: 2368
  to:
    kind: Service
    name: ghost
    weight: 100
  wildcardPolicy: None
