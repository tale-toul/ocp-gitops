---
# tasks file for bootstrap_cert_manager
- name: Bootstrap Cert Manager CA cluster issuer
  k8s:
    api_key: "{{ token }}"
    definition: "{{ lookup('file', 'App-bootstrap_cert_manager.yaml') }}" 
    state: "present"
